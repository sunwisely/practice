/*input diagram
 *2022/9/6
 zhiyang*/

#include <stdio.h>
#include <stdlib.h>

int main (void){
  int n,m;
  for(n=1;n<=4;n++)
    {
      for(m=4-n;m>0;m--)printf("_");
      for(m=1;m<=2*n-1;m++)printf("*");
      for(m=4-n;m>0;m--)printf("_");
      printf("\n");
    }
  return EXIT_SUCCESS;
}
